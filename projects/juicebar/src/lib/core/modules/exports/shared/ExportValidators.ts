import {Validators} from '@angular/forms';
import type {ValidatorFn} from '@angular/forms';

/**
 * Add your custom validators here then reference them in your {@link FilterValidator} key in your backend.
 * Validators must be static methods, and they must return a {@link ValidatorFn} or be a {@link ValidatorFn}.
 */
export class ExportValidators extends Validators {
}